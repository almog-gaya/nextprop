import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'AI Agent Configuration - NextProp',
  description: 'Configure your AI Agent',
};

export default function AIAgentLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
} 