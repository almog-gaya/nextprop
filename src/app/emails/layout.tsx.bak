'use client';

import React from 'react';
import { InstantlyProvider } from '@/contexts/InstantlyContext';
import DashboardLayout from '@/components/DashboardLayout';

export default function EmailsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <InstantlyProvider>
      {children}
    </InstantlyProvider>
  );
}